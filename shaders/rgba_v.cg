void main(
	float3 aPosition,
	float4 aColor,
	uniform float4x4 wvp,
	out float4 vPosition: POSITION,
	out float4 vColor: COLOR)
{
  float4x4 twvp = transpose(wvp);
	vPosition = mul(float4(aPosition, 1.f), twvp);
	vColor = aColor;
}

